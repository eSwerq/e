#include <iostream>
using namespace std;

namespace Tasks {
	int average (int source[]);
	bool isHeGonnaSurvive(int bullets, int dragons);					
	string reversedStrings(string source);
	int returnDubElement(int source[]);	
	bool allianceHelp(int soldiersHealth[], int soldiersAttach[], int monsterHealth, int monsterAttack);
};

